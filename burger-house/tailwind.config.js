/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // dark mode por clase (como ya usas `dark:` en el body)
  darkMode: "class",
  theme: { extend: {} },
  plugins: [],
}
